# Dependencies for Dictation Module

## Python Dependencies
requirements/dictation.txt

## System Dependencies
See: docs/architecture/SYSTEM_DEPS.md

Required packages:
- portaudio (sudo pacman -S portaudio)
- xdotool (sudo pacman -S xdotool)
- libnotify (sudo pacman -S libnotify)

## Setup
From project root:
  cd $AUTOMATION_SCRIPTS_DIR
  source scripts/setup-dev.sh dictation

